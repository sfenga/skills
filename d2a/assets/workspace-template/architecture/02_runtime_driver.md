<!-- d2a:pending -->
# S2 运行驱动

基于真实仓库回答以下问题。优先输出紧凑的结构化视图，避免冗长解释；初稿完成后依次进行压缩修订、去术语修订和口语化简化修订。
同时把本阶段主要结论及真实文件、符号、证明内容和置信度写入 `architecture/evidence/02_runtime_driver.json`。

## 主驱动

主导运行时驱动是什么？记录其入口、触发方式和代码证据。

## 核心运行循环

核心运行循环是什么？按实际执行顺序说明输入如何被接收、推进并产生结果。

## 引擎模块

哪个模块是引擎模块？说明它为何主导运行时，并记录真实文件与符号。

## 支撑模块

理解引擎所需的最多两个支撑模块是什么？说明其职责、与引擎的关系及代码证据。
